<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.MijnProject"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">

    <!-- SplashScreen (LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
           >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <!-- Login (geen LAUNCHER, wordt via SplashActivity gestart) -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="false" />

        <!-- Dashboard (alleen bereikbaar na inloggen) -->
        <activity
        android:name=".Dashboard"
        android:exported="true" />  <!-- Moet true zijn als je er vanaf Login naartoe gaat -->

    <!-- Overige activities -->
    <activity
        android:name=".Activity.ListDetailActivity"
        android:exported="false" />
    <activity
        android:name=".Activity.FavoritePage"
        android:exported="false" />
    <activity
        android:name=".Activity.ProfileActivity"
        android:exported="false" />
    <activity
        android:name=".Activity.AddList"
        android:exported="false" />
    <activity
        android:name=".Activity.ForgotPassword"
        android:exported="false" />
    <activity
        android:name=".Activity.SignupActivity"
        android:exported="false"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

    </activity>

    </application>
    </manifest>